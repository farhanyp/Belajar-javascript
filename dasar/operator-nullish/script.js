// Memakai nullish operator

let data;

// Mengecek apakah datanya null atau undifiend, jika null atau undifiend maka akan diganti
let checkTypeData = data ?? "ini data default"

console.log (checkTypeData)